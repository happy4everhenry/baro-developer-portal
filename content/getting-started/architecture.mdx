# Architecture <KoreanLabel>아키텍처</KoreanLabel>

<PageMeta status="live" layer="shared" />

BARO is a **three-layer system**: an iOS app (SwiftUI), a Python backend (FastAPI), and an AI platform (LangGraph + Claude). This page explains how each layer works and how they all fit together.

<InfoBox type="fun" title="Think of it like a restaurant">
**Views** are the menu (what customers see). **ViewModels** are the waiters (they take orders and deliver food). **Repositories** are the kitchen (they prepare the actual data). The **Backend** is the supply chain that delivers fresh ingredients from the market. The **AI Platform** is the Michelin-star consultant analyzing trends and recommending new dishes.
</InfoBox>

---

<Tabs>

<Tab label="Big Picture" icon="🗺️">

## The Full Architecture

This diagram shows every layer of BARO — from the iOS screens you see, through the Python backend, down to external data sources and the AI platform.

<Diagram title="BARO Architecture — All Layers">
  <DiagramRow>
    <DiagramGroup label="iOS Presentation Layer" color="jade">
      <DiagramNode type="view" icon="📱" label="HomeView" sublabel="Main screen" />
      <DiagramNode type="view" icon="🔍" label="SearchView" sublabel="Stock search" />
      <DiagramNode type="view" icon="📈" label="StockDetailPageView" sublabel="Detail screen" />
      <DiagramNode type="view" icon="🧭" label="MainTabView" sublabel="Navigation" />
      <DiagramNode type="view" icon="🔐" label="LoginView" sublabel="Authentication" />
      <DiagramNode type="view" icon="🧠" label="AIInsightsView" sublabel="AI Hub" />
    </DiagramGroup>
  </DiagramRow>
  <DiagramArrow direction="down" label="@Observable binding" />
  <DiagramRow>
    <DiagramGroup label="iOS Business Logic" color="blue">
      <DiagramNode type="vm" icon="🧠" label="HomeViewModel" />
      <DiagramNode type="vm" icon="🧠" label="SearchViewModel" />
      <DiagramNode type="vm" icon="🧠" label="StockDetailPageViewModel" />
      <DiagramNode type="vm" icon="🔐" label="AuthManager" />
    </DiagramGroup>
  </DiagramRow>
  <DiagramArrow direction="down" label="async/await" />
  <DiagramRow>
    <DiagramGroup label="iOS Data Access" color="purple">
      <DiagramNode type="repo" icon="📚" label="MarketRepository" sublabel="Protocol" />
      <DiagramNode type="repo" icon="📚" label="BannerRepository" sublabel="Protocol" />
      <DiagramNode type="repo" icon="📚" label="NewsRepository" sublabel="Protocol" />
      <DiagramNode type="repo" icon="📚" label="EventRepository" sublabel="Protocol" />
    </DiagramGroup>
  </DiagramRow>
  <DiagramArrow direction="down" label="HTTP + WebSocket" />
  <DiagramRow>
    <DiagramGroup label="Python Backend (FastAPI)" color="blue">
      <DiagramNode type="service" icon="🐍" label="REST Routers" sublabel="auth, market, stock" />
      <DiagramNode type="service" icon="⚡" label="WebSocket Relay" sublabel="Stream manager" />
      <DiagramNode type="service" icon="🔐" label="Auth Service" sublabel="JWT + bcrypt" />
      <DiagramNode type="data" icon="🗄️" label="SQLite" sublabel="User database" />
    </DiagramGroup>
  </DiagramRow>
  <DiagramArrow direction="down" label="KIS OAuth2 + httpx" />
  <DiagramRow>
    <DiagramGroup label="Backend Services" color="amber">
      <DiagramNode type="service" icon="🔑" label="KIS Auth" sublabel="OAuth2 tokens" />
      <DiagramNode type="service" icon="📊" label="KIS Stock Service" sublabel="Quote, chart, orderbook" />
      <DiagramNode type="service" icon="🌍" label="KIS Market Service" sublabel="Indices + FX rates" />
      <DiagramNode type="service" icon="📡" label="KIS WebSocket" sublabel="Real-time market feed" />
    </DiagramGroup>
  </DiagramRow>
  <DiagramArrow direction="down" label="external APIs" />
  <DiagramRow>
    <DiagramGroup label="External Data Sources" color="amber">
      <DiagramNode type="data" icon="🇰🇷" label="KIS Open API" sublabel="Korean stock exchange" />
      <DiagramNode type="data" icon="🌐" label="Yahoo Finance" sublabel="Global indices + FX" />
    </DiagramGroup>
  </DiagramRow>
</Diagram>

<Diagram title="AI Platform Layer (Phase 3 — In Progress)">
  <DiagramRow>
    <DiagramGroup label="12 AI Agents (LangGraph Orchestrated)" color="purple">
      <DiagramNode type="vm" icon="📈" label="Trade Signal" sublabel="Buy/sell/hold" />
      <DiagramNode type="vm" icon="🛡️" label="Risk Guard" sublabel="AML + fraud" />
      <DiagramNode type="vm" icon="💬" label="AI Chatbot" sublabel="Korean Q&A" />
      <DiagramNode type="vm" icon="📰" label="News Sentiment" sublabel="KoBERT + FinBERT" />
      <DiagramNode type="vm" icon="🔔" label="Smart Notification" sublabel="Relevance scoring" />
      <DiagramNode type="vm" icon="🏗️" label="Control Tower" sublabel="Watches all agents" />
    </DiagramGroup>
  </DiagramRow>
  <DiagramArrow direction="down" label="tools + retrieval" />
  <DiagramRow>
    <DiagramGroup label="AI Infrastructure" color="purple">
      <DiagramNode type="service" icon="🧬" label="LangGraph" sublabel="State graphs + HITL" />
      <DiagramNode type="service" icon="📡" label="RAG Pipeline" sublabel="8-stage retrieval" />
      <DiagramNode type="service" icon="🔧" label="6 MCP Servers" sublabel="38 tools" />
      <DiagramNode type="service" icon="🔒" label="4-Layer Firewall" sublabel="Input/output/inter-agent" />
    </DiagramGroup>
  </DiagramRow>
  <DiagramArrow direction="down" label="models + storage" />
  <DiagramRow>
    <DiagramGroup label="AI Models & Data" color="purple">
      <DiagramNode type="data" icon="🧠" label="Claude (Anthropic)" sublabel="Primary LLM" />
      <DiagramNode type="data" icon="🇰🇷" label="EXAONE 3.5" sublabel="Local Korean LLM" />
      <DiagramNode type="data" icon="📚" label="pgvector" sublabel="Vector embeddings" />
      <DiagramNode type="data" icon="⏱️" label="TimescaleDB" sublabel="Time-series data" />
    </DiagramGroup>
  </DiagramRow>
</Diagram>

</Tab>


<Tab label="Each Layer" icon="🧅">

## Layer-by-Layer Breakdown

<Accordion>

<AccordionItem title="Presentation Layer — What you see" icon="📱" badge="Views">

These are the SwiftUI screens the user interacts with. They don't contain any business logic — they just display what the ViewModel tells them to show.

<InfoBox type="info" title="Analogy">
Views are like **TV screens**. They show whatever signal (data) is sent to them, but they don't decide what's on TV.
</InfoBox>

**Key views:**
- `HomeView` — The main screen with stock rankings, indices, and banners
- `SearchView` — Type a stock name or code to find it
- `StockDetailPageView` — Full detail page for a single stock
- `MainTabView` — The tab bar at the bottom that switches between screens

</AccordionItem>


<AccordionItem title="Business Logic Layer — The brains" icon="🧠" badge="ViewModels">

ViewModels sit between the View and the data. They fetch data from Repositories, process it, and expose it to Views using Swift's `@Observable` macro.

<InfoBox type="info" title="Analogy">
ViewModels are like **TV producers**. They decide what content to show, gather it from different sources, and send it to the screen.
</InfoBox>

**Key ViewModels:**
- `HomeViewModel` — Loads market overview, top stocks, banners, and news
- `SearchViewModel` — Handles search queries and filters results
- `StockDetailPageViewModel` — Manages stock detail, chart settings, and real-time price updates

</AccordionItem>

<AccordionItem title="Data Access Layer — The librarians" icon="📚" badge="Repositories">

Repositories are defined as **protocols** (interfaces). This means you can swap between fake data and real API data without changing any other code.

<InfoBox type="tip" title="Analogy">
A Repository is like a **librarian**. You ask for a book (data), and the librarian knows exactly where to find it — whether it's on the shelf (mock data) or needs to be ordered from another library (API call). You don't need to know how they got it.
</InfoBox>

**Repository protocols:**
- `MarketRepositoryProtocol` — Stocks, indices, market overview
- `BannerRepositoryProtocol` — Promotional banners
- `NewsRepositoryProtocol` — Market news articles

**Each protocol has two implementations:**
- `Mock___Repository` — Returns hardcoded sample data (great for development and previews)
- `API___Repository` — Makes real HTTP calls to the backend

</AccordionItem>

<AccordionItem title="Real-time Layer — Live prices" icon="⚡" badge="WebSocket">

BARO uses WebSockets for live stock price updates. The data flows through three hops:

<Diagram title="Real-time data flow">
  <DiagramRow>
    <DiagramNode type="service" icon="📊" label="KIS WebSocket" sublabel="Korean market feed" />
  </DiagramRow>
  <DiagramArrow direction="down" label="raw market data" />
  <DiagramRow>
    <DiagramNode type="service" icon="🐍" label="FastAPI WS Relay" sublabel="Backend parses & forwards" />
  </DiagramRow>
  <DiagramArrow direction="down" label="clean JSON" />
  <DiagramRow>
    <DiagramNode type="service" icon="📱" label="WebSocketManager" sublabel="iOS app receives updates" />
  </DiagramRow>
</Diagram>

<InfoBox type="info" title="Why a relay?">
The KIS WebSocket sends raw binary data in a Korean-market-specific format. Our Python backend translates it into clean JSON that the iOS app can easily understand.
</InfoBox>

</AccordionItem>


<AccordionItem title="Domain Layer — The data shapes" icon="💎" badge="Models">

Models define the shape of the data that flows through the app. They're plain Swift structs — no logic, just data.

<InfoBox type="info" title="Analogy">
Models are like **blueprints**. A `Stock` model says "every stock has a name, a price, and a change percentage" — but it doesn't know how to fetch or display that data.
</InfoBox>

**Key models:**
- `Stock` — Stock code, name, price, change, volume
- `MarketIndex` — KOSPI, KOSDAQ index values
- `StockDetailInfo` — Extended stock information for detail pages
- `PriceUpdate` — Real-time price tick from WebSocket
- `ChartSettings` — User's chart configuration (time range, chart type)

</AccordionItem>

<AccordionItem title="Python Backend — The server" icon="🐍" badge="FastAPI">

The backend is a Python FastAPI server that sits between the iOS app and external data sources. It handles authentication, data processing, and real-time streaming.

<InfoBox type="info" title="Analogy">
The backend is like a **supply chain**. The iOS app orders ingredients (stock data), and the backend sources them from the right supplier (KIS API for Korean stocks, Yahoo Finance for global indices) and delivers them in a clean package (JSON).
</InfoBox>

**Backend components:**
- **Routers** — `auth.py`, `market.py`, `stock.py`, `websocket.py` — handle incoming requests
- **Services** — `kis_auth.py`, `kis_stock_service.py`, `kis_market_service.py` — talk to KIS API
- **Auth Service** — JWT tokens, password hashing with bcrypt, Apple Sign In verification
- **Stream Manager** — Reference-counted KIS WebSocket subscriptions with fan-out to iOS clients
- **Database** — SQLite for user accounts and refresh tokens

**Key numbers:**
- 25+ REST endpoints across 4 routers
- 2 WebSocket endpoints (streaming + status)
- 26 global market indices tracked
- 21 FX pairs vs KRW
- Rate limited to 15 concurrent KIS API calls

</AccordionItem>

<AccordionItem title="AI Platform — The intelligent layer" icon="🤖" badge="Phase 3">

The AI Platform is being built on top of the existing architecture. It adds 12 specialized agents that analyze markets, detect risks, and generate insights.

<InfoBox type="info" title="Analogy">
The AI Platform is like a team of **12 expert consultants**, each with a specialty. One analyzes market trends, another watches for fraud, another writes daily reports. They all work under a Control Tower that makes sure they play nice together.
</InfoBox>

**7-Layer Technology Stack:**
1. **Infrastructure** — Vercel, Railway Seoul, Supabase, Redis, Cloudflare
2. **Models** — Claude (Anthropic), XGBoost, KoBERT, FinBERT-KO, BGE-M3
3. **Data** — TimescaleDB, pgvector, KIS API, Redis caching
4. **Orchestration** — LangGraph typed state graphs, BullMQ, Redis Pub/Sub
5. **Application** — Next.js, React Native, TradingView Charts
6. **MCP** — 6 MCP Servers with 38 standardized tools
7. **LangGraph** — Agent orchestration with checkpointing and human-in-the-loop

**12 AI Agents:**
- Trade Signal, AI Advisor Chatbot, Risk Guard, Market Analysis
- Portfolio Optimizer, Smart Notification, News Sentiment, Document Analysis
- CUI Agent (voice), Feature Request, Security Monitor, Control Tower

**Knowledge System (CAG + RAG):**
- **CAG** — Cached context (regulations, glossary) — 0ms retrieval
- **RAG** — 8-stage semantic search pipeline — 100ms–2s retrieval
- **Local LLM** — EXAONE 3.5 7.8B for cost-efficient Korean processing

**Safety:**
- 4-layer firewall (input, output, inter-agent, training pipeline)
- Human-in-the-loop on all financial actions
- 5-pillar governance (alignment, control, visibility, security, compliance)

</AccordionItem>
</Accordion>

</Tab>

<Tab label="Code Examples" icon="💻">

## How It Works in Code

<Accordion>

<AccordionItem title="Dependency Injection — Switching between mock and real data" icon="🔌">

All services are injected through a `DependencyContainer` that's passed via SwiftUI's environment. Flip the `useMocks` flag to switch between fake and live data.

```swift filename="DependencyContainer.swift"
@Observable
final class DependencyContainer {
    let marketRepository: any MarketRepositoryProtocol
    let bannerRepository: any BannerRepositoryProtocol
    let newsRepository: any NewsRepositoryProtocol

    init(useMocks: Bool = true) {
        if useMocks {
            self.marketRepository = MockMarketRepository()
            self.bannerRepository = MockBannerRepository()
            self.newsRepository = MockNewsRepository()
        } else {
            self.marketRepository = APIMarketRepository()
            self.bannerRepository = MockBannerRepository()
            self.newsRepository = MockNewsRepository()
        }
    }
}
```

<InfoBox type="tip" title="Why start with mocks?">
During development, mocks let you work on the UI without needing the backend server running. When you're ready to test with real data, just set `useMocks: false`.
</InfoBox>

</AccordionItem>


<AccordionItem title="API Client — How the app talks to the backend" icon="🌐">

The `APIClient` is a simple, reusable struct that makes HTTP GET requests and decodes JSON responses.

```swift filename="APIClient.swift"
struct APIClient: Sendable {
    let baseURL: URL
    // Simulator: 127.0.0.1:8000
    // Device:    192.168.68.57:8000

    func get<T: Decodable>(
        _ path: String,
        queryItems: [URLQueryItem]? = nil
    ) async throws -> T {
        // 1. Build URL with path + query params
        // 2. Validate URL → throws APIError.invalidURL
        // 3. Fetch data via URLSession
        // 4. Validate HTTP 200-299 → throws APIError.httpError
        // 5. Decode JSON (snakeCase strategy) → returns T
    }
}

enum APIError: LocalizedError {
    case invalidURL
    case invalidResponse
    case httpError(statusCode: Int)
}
```

<InfoBox type="info" title="What's Sendable?">
`Sendable` is a Swift 6 feature that tells the compiler this struct is safe to use across different threads. Since network requests run in the background, this is important!
</InfoBox>

</AccordionItem>
</Accordion>

</Tab>
</Tabs>
